export type message = {
  board_id: any;
  dateTime: string | null;
  content: string | null;
};

export type messageFront = {
  messageFront: message;
};

// export type ListProps = { data: message[] };
